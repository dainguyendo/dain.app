import { styled } from "../../stitches.config";

export const Button = styled("button", {
  border: "none",
  borderRadius: "$1",

  p: "$1",
  fontFamily: "$untitled",
  fontSize: "$2",
  fontWeight: 700,
});
